# Dependências de produção — apenas o que é utilizado
fastapi==0.104.0
uvicorn[standard]==0.24.0

# Base de dados
sqlalchemy==2.0.23
psycopg[binary]>=3.1,<4

# Validação
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0
python-multipart==0.0.6

# Autenticação
PyJWT==2.8.0
bcrypt==4.0.1

# Utilitários
python-dotenv==1.0.0
openpyxl==3.1.2
xhtml2pdf>=0.2.11

# Storage (Backblaze B2 via API S3-compatible)
boto3==1.34.0

# Scheduler in-process (lembretes de reserva — Sprint 6 Lavagem)
apscheduler==3.10.4

# HTTP client (webhook Brevo Conversations — envio de contexto)
httpx==0.28.1
